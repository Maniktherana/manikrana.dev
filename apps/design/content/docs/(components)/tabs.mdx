---
title: Tabs
description: Tabbed interface styled as compact rails.
component: true
base: base
---

<ComponentPreview name="tabs-demo" />

## Installation

```bash
bunx shadcn@latest add tabs
```

## Usage

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
```

```tsx
<Tabs defaultValue="preview">
  <TabsList variant="underline">
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">Preview panel</TabsContent>
  <TabsContent value="code">Code panel</TabsContent>
</Tabs>
```

## Examples

### Default

Use the default variant for a filled segmented control with an animated indicator.

<ComponentPreview name="tabs-default" />

### Underline

Use `variant="underline"` for compact navigation inside panels.

<ComponentPreview name="tabs-underline" />

### Ghost

Use `variant="ghost"` for transparent rails with a moving filled active state.

<ComponentPreview name="tabs-ghost" />

### Vertical

Set `orientation="vertical"` on `Tabs` when the list should sit beside the panel.

<ComponentPreview name="tabs-vertical" />
