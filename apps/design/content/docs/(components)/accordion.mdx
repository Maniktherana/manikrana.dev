---
title: Accordion
description: Base UI accordion restyled as a compact card row.
component: true
base: base
---

<ComponentPreview name="accordion-demo" />

## Installation

```bash
bunx shadcn@latest add accordion
```

## Usage

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";
```

```tsx
<Accordion defaultValue={["shipping"]}>
  <AccordionItem value="shipping">
    <AccordionTrigger>Shipping settings</AccordionTrigger>
    <AccordionContent>Configure fulfillment windows and carrier defaults.</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Examples

### Grouped

Use `variant="grouped"` when accordion items should share one filled group and separate as they open.

<ComponentPreview name="accordion-grouped" />

### Multiple

Use `defaultValue` with several item values when multiple sections should open
by default.

<ComponentPreview name="accordion-multiple" />

### Status

Compose `AccordionTrigger` with inline badges or other text primitives.

<ComponentPreview name="accordion-status" />
