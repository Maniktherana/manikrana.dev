---
title: Modal
description: A focused prompt dialog for destructive, confirmative, and feedback workflows.
component: true
base: base
---

<ComponentPreview name="modal-demo" />

## Installation

```bash
bunx shadcn@latest add dialog
```

## Usage

```tsx
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogBody,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
```

```tsx
<Dialog>
  <DialogTrigger render={<Button variant="secondary" />}>Open modal</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Delete something</DialogTitle>
      <DialogDescription>Are you sure? This cannot be undone.</DialogDescription>
    </DialogHeader>
    <DialogBody>
      <p>
        Please type <strong>delete</strong> to confirm.
      </p>
      <Input placeholder="Confirmation text" />
    </DialogBody>
    <DialogFooter>
      <DialogClose render={<Button variant="ghost" />}>Cancel</DialogClose>
      <Button variant="destructive">Delete</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

## Examples

### Destructive

Use `DialogBody` for additional confirmation content between the header and footer.

<ComponentPreview name="modal-demo" />

### Confirmative

Use only the header and footer for shorter prompts.

<ComponentPreview name="modal-confirmative" />

## API Reference

### DialogContent

| Prop | Type | Default |
| --- | --- | --- |
| `showCloseButton` | `boolean` | `false` |
| `className` | `string` | - |

### DialogHeader

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |

### DialogBody

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |

### DialogFooter

| Prop | Type | Default |
| --- | --- | --- |
| `showCloseButton` | `boolean` | `false` |
| `className` | `string` | - |
