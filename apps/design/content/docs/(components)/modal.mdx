---
title: Dialog
description: A focused dialog for destructive, confirmative, and feedback workflows.
component: true
base: base
---

<ComponentPreview name="modal-demo" />

## Installation

```bash
bunx shadcn@latest add dialog
```

## Usage

```tsx
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogBody,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
```

```tsx
<Dialog>
  <DialogTrigger render={<Button variant="outline" />}>Open dialog</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Delete something</DialogTitle>
      <DialogDescription>Are you sure? This cannot be undone.</DialogDescription>
    </DialogHeader>
    <DialogBody>
      <p>
        Please type <strong>delete</strong> to confirm.
      </p>
      <Input placeholder="Confirmation text" />
    </DialogBody>
    <DialogFooter>
      <DialogClose render={<Button variant="ghost" />}>Cancel</DialogClose>
      <Button variant="destructive">Delete</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

## Examples

### Destructive

Use `DialogBody` for additional confirmation content between the header and footer.

<ComponentPreview name="modal-demo" />

### Confirmative

Use only the header and footer for shorter prompts.

<ComponentPreview name="modal-confirmative" />
