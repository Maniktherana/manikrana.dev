---
title: Input Group
description: Composable grouped inputs with addons, inline actions, sizes, radius, and state handling.
component: true
base: primitive
---

<ComponentPreview name="search-demo" />

## Installation

`InputGroup` is the primitive for inputs that need leading icons, trailing
actions, keyboard hints, helper text, or multiline addons inside one control
surface.

## Usage

```tsx
import { InputGroup, InputGroupAddon, InputGroupInput } from "@/components/ui/input-group";
```

```tsx
<InputGroup variant="component">
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
  <InputGroupInput placeholder="Search products" />
</InputGroup>
```

## Examples

### Small

Use `size="sm"` for dense toolbars and sidebars.

<ComponentPreview name="search-small" />

### Rounded

Use `radius="rounded"` when the field sits as a standalone chip.

<ComponentPreview name="search-rounded" />

### State

Use `variant`, `aria-invalid`, and disabled controls for stateful grouped
inputs. Invalid state belongs to the outer group border; addons and internal
controls keep their normal separator color.

<ComponentPreview name="search-state" />
