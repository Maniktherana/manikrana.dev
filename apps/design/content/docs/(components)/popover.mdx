---
title: Popover
description: Floating inverted content with title, description, close, arrow, and footer slots.
component: true
base: base
---

<ComponentPreview name="popover-demo" />

## Installation

```bash
bunx shadcn@latest add popover
```

## Usage

```tsx
import { Button } from "@/components/ui/button";
import {
  Popover,
  PopoverContent,
  PopoverDescription,
  PopoverFooter,
  PopoverFooterActions,
  PopoverHeader,
  PopoverSeparator,
  PopoverStep,
  PopoverTitle,
  PopoverTrigger,
} from "@/components/ui/popover";
```

```tsx
<Popover>
  <PopoverTrigger>Open popover</PopoverTrigger>
  <PopoverContent showArrow showCloseButton>
    <PopoverHeader>
      <PopoverTitle>Insert Popover</PopoverTitle>
      <PopoverDescription>
        Insert popover description here. It would look much better as three lines of text.
      </PopoverDescription>
    </PopoverHeader>
    <PopoverSeparator />
    <PopoverFooter>
      <PopoverStep>Step 1 of 5</PopoverStep>
      <PopoverFooterActions>
        <Button type="button" variant="ghost" size="sm">
          Back
        </Button>
        <Button type="button" size="sm" className="flex-1">
          Next
        </Button>
      </PopoverFooterActions>
    </PopoverFooter>
  </PopoverContent>
</Popover>
```

## Examples

### Footer

Use `PopoverFooter`, `PopoverStep`, and `PopoverFooterActions` for stepper-style popovers.

<ComponentPreview name="popover-footer" />

### Stretch

Use flex layout on standard `Button` actions when the footer has only actions.

<ComponentPreview name="popover-stretch" />

### Sides

Use `side="left"` or `side="right"` for side-mounted popovers.

<ComponentPreview name="popover-sides" />

## API Reference

### Popover

`Popover` is the Base UI popover root.

| Prop | Type | Default |
| --- | --- | --- |
| `defaultOpen` | `boolean` | - |
| `open` | `boolean` | - |
| `onOpenChange` | `(open: boolean) => void` | - |

### PopoverContent

| Prop | Type | Default |
| --- | --- | --- |
| `side` | `"top" \| "bottom" \| "left" \| "right" \| "inline-start" \| "inline-end"` | `"bottom"` |
| `sideOffset` | `number` | `16` |
| `align` | `"start" \| "center" \| "end"` | `"center"` |
| `alignOffset` | `number` | `0` |
| `showArrow` | `boolean` | `false` |
| `showCloseButton` | `boolean` | `false` |
| `className` | `string` | - |
