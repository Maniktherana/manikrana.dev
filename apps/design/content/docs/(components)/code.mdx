---
title: Code
description: Inline code token aligned to compact monospace labels.
component: true
base: local
---

<ComponentPreview name="code-demo" />

## Installation

`Code` is a local docs/design primitive.

## Usage

```tsx
import { Code } from "@/components/design/code";
```

```tsx
Use <Code>order.total</Code> inside prose.
```

## Examples

### Background

Use `background="muted"` when the inline token sits inside a heavier surface.

<ComponentPreview name="code-background" />

### Inline

Use `Code` inside text for prop names, file fragments, and compact tokens.

<ComponentPreview name="code-inline" />

## API Reference

### Code

| Prop | Type | Default |
| --- | --- | --- |
| `background` | `"default" \| "muted"` | `"default"` |
| `children` | `React.ReactNode` | - |
| `className` | `string` | - |
